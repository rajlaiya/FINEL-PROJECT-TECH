[build]
  command = "npm ci && npx vite build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--legacy-peer-deps"

# Ensure client-side routing works for the Vite SPA
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
